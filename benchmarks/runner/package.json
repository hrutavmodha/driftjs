{
  "name": "driftjs-benchmark-runner",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/cli.ts",
    "bench": "tsx src/cli.ts",
    "app": "pnpm --filter driftjs-benchmark-app dev",
    "build:app": "pnpm --filter driftjs-benchmark-app build"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "playwright": "^1.62.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
