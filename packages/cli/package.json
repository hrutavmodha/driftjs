{
  "name": "create-drift",
  "version": "0.0.3",
  "description": "CLI scaffolding tool for DriftJS register VM applications",
  "type": "module",
  "files": [
    "bin",
    "dist",
    "template"
  ],
  "bin": {
    "create-drift": "./bin/create-drift.js",
    "drift": "./bin/create-drift.js"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "node -e \"const fs = require('fs'); fs.rmSync('./template', { recursive: true, force: true }); fs.cpSync('../../template', './template', { recursive: true, filter: (src) => !src.includes('node_modules') && !src.includes('dist') });\"",
    "build": "pnpm prebuild && vite build",
    "test": "vitest run",
    "prepublishOnly": "pnpm build && pnpm test"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
