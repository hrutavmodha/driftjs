{
  "name": "driftjs-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --recursive run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
