{
  "name": "driftjs-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "publish": "pnpm -r publish --no-git-checks --access public",
    "patch": "node scripts/version/patch.js",
    "minor": "node scripts/version/minor.js",
    "major": "node scripts/version/major.js"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
